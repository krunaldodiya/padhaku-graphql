input AttachmentInput {
    id: ID
    source: String
    path: String!
    thumbnail: String!
    size: String!
    mime: String!
    height: String!
    width: String!
    status: Boolean
}

input QuizMetaInput {
    quiz_id: ID!
    question_id: ID!
    current_answer: String!
    answer: String!
    seconds: Int!
}

input ChecksumInput {
    ORDER_ID: ID!
    MID: String!
}
